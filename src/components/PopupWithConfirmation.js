import { Popup } from "./Popup.js";

    export class PopupWithConfirmation extends Popup {
        constructor (popupSelector) {
            super (popupSelector);
        }

    // confirmDeleting() {

    // }    

    // setEventListeners() {
    //     super.setEventListeners();
    //     this._popup.addEventListener('click', this.confirmDeleting); 
    // }
}    